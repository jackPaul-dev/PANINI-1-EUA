[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm install -g pnpm@10.26.1", "pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["echo 'dist pre-built — skip'"]

[start]
cmd = "node --enable-source-maps artifacts/api-server/dist/index.mjs"
